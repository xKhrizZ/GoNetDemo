# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GoNetDemo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GoNetDemo
  pod 'iOSApiRest', :path => '../iOSApiRest'
  #pod 'iOSDataPersistence', :path => '../iOSDataPersistence'
  pod 'iOSSecurity', :path => '../iOSSecurity'
  pod 'iOSBusinessDomain', :path => '../iOSBusinessDomain'

  target 'GoNetDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GoNetDemoUITests' do
    # Pods for testing
  end

end
